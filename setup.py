from setuptools import setup, find_packages

setup(
    name="scopeUtils",
    version="0.1.0",
    packages=find_packages(),
    author="Anshu Chen",
    description="Utilities and helper functions for creating firm-level scope classifications, as described in Chen (2025)",
    python_requires='>=3.12.3', # probably doesn't have to be this high, but I used this version to develop
    url='https://github.com/your_username/my_package' # TODO update with real link
    )

